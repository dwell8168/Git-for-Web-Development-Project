https://codepen.io/dblack8168/pen/WNQJmyY?editors=1100

    1. What is Semantic HTML? 
   -    It’s a markup which basically means it’s introduces meaning to the web page.
    2. What is HTML used for? 
   -  It’s used to create pages on the web and give them a bit of their identity. 
    3. What is an attribute and where do we put it? 
   - Attributes are special words used inside the opening tag to control the element's behavior.
    4. What is the h1 tag used for? How many times should I use it on a page?
   - It is the header tag and it is usually the largest text and it stands out. You should use it once to  stand out but you can use more times than that if it makes sense.
    5. Name two tags that have required attributes
    -<p> <id>
    6. What do we put in the head of our HTML document? 
    - It can include can a title for the document, scripts, styles, meta information, etc.
    7. What is an id? 
    - This allows you to set a particular style for many HTML elements with the same class.
    8. What elements can I add an id to? 
    - You can use anchors.
    9. How many times can I use the same id on a page? 
    - It’s only used once per page
    10. What is a class? 
    - The class attribute specifies one or more class names for an element. The class attribute is mostly used to point to a class in a style sheet. 
    11. What elements can I add a class to? 
    - You can add a div and section element along with other options.
    12. How many times can I use the same class on a page? 
    - You can use the same class multiple times on a page.
    13. How do I get my link to open in a new tab?
    - You use the <a> tag with the href attribute to indicate the address of the target page. Example: <a href="http://www.google.com"> . You can make a link to another page in your website simply by writing the file name: <a href="page2.html"> .
    14. What is the alt attribute in the image tag used for? 
    - The alt attribute provides alternative information for an image if a user for some reason cannot view it.
    15. How do I reference an id?
    - You would have to call it.
    16. What is the difference between a section and a div
    - The <section> tag defines sections in a document, such as chapters, headers, footers, or any other sections of the document. whereas: The <div> tag defines a division or a section in an HTML document. The <div> tag is used to group block-elements to format them with CSS.
    17. What is CSS used for? 
    - CSS is the language for describing the presentation of Web pages, including colors, layout, and fonts.
    18. How to we select an element? Example - every h2 on the page
    - You can use classes, divs, or an id.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    - class can be used to select certain content on the page or define certain content numerous times where id is a special occasion and only used once.
    20. How do we select classes in CSS?
    - To select elements with a specific class, write a period (.) character, followed by the name of the class.
    21. How do we select a p element with a single class of “human””?
    - It would be p.human
    22. What is a parent child selector? When would this be useful? 
    - A parent child selector selects all elements that are a direct child of the specified element. It can be useful inside a specific class or div.
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector?
    - A pseudo-class is a selector that selects elements that are in a specific state, e.g. they are the first element of their type, or they are being hovered over by the mouse pointer.
    25. What do we use the change the spacing between lines?
    - We can use tab.
    26. What do we use to change the spacing between letters?
    - We would use the spacebar.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    We could use text-transform in CSS then put lowercase, uppercase, or capitalize.
    28. How do I add a 1px border around my div that is dotted and black?
    - border: 1 px dotted black;
    29. How do I select everything on the page? 
    - CTRL + A
    30. How do I write a comment in CSS?
    You would want to type in two back slashes before commenting so it will not affect anything else.
    31. How do I find out what file I am in, when I am using the command line? 
    - It should show as the master file.
    32. Using the command line - how do I see a list of files/folders in my current folder?
    - Go into documents and see.
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    - You can look to backspace.
    34. Why should I use version control? 
    - You should use it to update your work, and push it to GitHub.
    35. How often should I commit to github?
    - Anytime a project is updated and is new. Or as often as you deem necessary.
    36. What is the command we would use to push our repo up to github? 
    - git push origin 'david-blackwell' is the commands you would use.
    37. Walk me through Lambda's git flow. 
      - Fork
      - Clone
      - cd to your Lambda folder
      - git clone https://url
      - cd into the newly cloned repo
      - git checkout -b 'david-blackwell'
      - Make a small change to any of the files
      - git add .
      - git commit -m "commit message"
      - git push origin 'david-blackwell'

